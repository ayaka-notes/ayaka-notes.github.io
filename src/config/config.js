const Config = {
    // 根据开发环境或者生产环境配置
    // API_BaseURL: 'http://localhost:8080', // 开发环境
    // API_BaseURL: '/api', // 生产环境，nginx代理
    // API_BaseURL: process.env.NODE_ENV === 'development' ? 'http://localhost:8080' : '/api',
    // Client_LocalToken_Key: '365-office-token',
    // Client_LocalToken_Expire: '365-office-token-expire',
    // Client_LocalToken_User: '365-office-token-user',
};

export default Config;

